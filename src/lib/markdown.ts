// Keep only the interface definition in this file

export interface ProjectFrontmatter {
	title: string;
	project: string;
	description: string;
	image: string;
	day: number;
	color: string;
}

// Functions moved to src/app/actions/projects.ts
